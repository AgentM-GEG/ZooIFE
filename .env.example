# Panoptes subject queue (see ZooniverseImageLoader + panoptesService).
# Workflow is required for /subjects/queued. Subject set is optional unless the workflow is "grouped".
VITE_ZOONIVERSE_WORKFLOW_ID=31510
# VITE_ZOONIVERSE_SUBJECT_SET_ID=
