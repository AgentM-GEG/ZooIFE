# SAM2 local inference server
#
# Full setup:
#   1. pip install torch torchvision  # match your CUDA version from pytorch.org
#   2. git clone https://github.com/facebookresearch/sam2 && pip install -e ./sam2
#   3. pip install -r server/requirements.txt
#   4. python server/sam2_server.py

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
Pillow>=10.0.0
numpy>=1.24.0
